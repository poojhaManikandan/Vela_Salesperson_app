void downloadCsvFile(String csvData, String fileName) {
  print('CSV download not supported natively on mobile without plugins.');
}
void downloadBytes(List<int> bytes, String fileName, {String mimeType = 'application/pdf'}) {
  print('Byte download not supported natively on mobile without plugins.');
}
